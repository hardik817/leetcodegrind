class Solution {
public:
    int minOperations(vector<int>& nums) {
        int and1=nums[0];
        int n=nums.size();
        sort(nums.begin(),nums.end());
        if(nums[0]==nums[n-1]) return 0;
        return 1;
    }
};